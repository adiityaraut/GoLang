package basics 

const pi = 3.14
const gravity = 9.8

func main {
	const days int = 7 // Local constant declaration with type
	const {
		monday=1
		tuesday=2
		wednesday=3
	} // Grouped constant declaration
	// Constants in Go are immutable and must be assigned a value at the time of declaration, with values that can be determined at compile time.
}